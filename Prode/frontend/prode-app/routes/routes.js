//const host = 'http://prode8.com.ar/Prode/backend/prode-api/';
const host = 'http://localhost/Prode/backend/prode-api/';


const login_services = host + "login.php";
function getLogin_services(){
    return login_services;
}
const registro_services = host + "registro.php";
function getRegistro_services(){
    return registro_services;
}

const enviarResultados_services = host + "enviarResultados.php";
function getEnviar_services(){
    return enviarResultados_services;
}

const verProde_services = host + "verProde.php";
function getProde_services(){
    return verProde_services;
}

const menu_services = host + "menu.php";
function getMenu_services(){
    return menu_services;
}

const resultados_services = host + "resultados.php";
function getResultados_services(){
    return resultados_services;
}